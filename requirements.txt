# Core web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.5.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# HTTP & Utilities
requests>=2.31.0

# Testing
pytest>=8.0.0
httpx>=0.27.0

# Inference Engine (CPU-optimized GGUF runner)
# In GitHub Actions (Linux x86_64), prebuilt wheels or standard build is installed.
# For local testing without GGUF, set MOCK_MODEL=true
llama-cpp-python>=0.2.75; sys_platform == 'linux'
